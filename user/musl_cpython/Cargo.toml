[package]
name = "musl_cpython"
version = "0.1.0"
edition = "2021"
build = "../../build_musl_cpython.rs"

[lib]
crate-type = ["dylib"]

[dependencies]
as_musl = { path = "../../as_musl" }

[features]
default = []
mpk = ["as_musl/mpk"]
