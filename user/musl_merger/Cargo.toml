[package]
name = "musl_merger"
version = "0.1.0"
edition = "2021"
build = "../../build_musl_user.rs"

[lib]
crate-type = ["dylib"]

[dependencies]
as_musl = { path = "../../as_musl" }

[features]
default = []
mpk = ["as_musl/mpk"]
